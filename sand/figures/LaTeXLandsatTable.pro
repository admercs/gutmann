;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;      Parses the input file into a structure;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;function readInputFile, inputfile        if not file_test(inputfile) then return, {n_images:-1}        openr, un, /get, inputfile        n_images=0        readf, un, n_images;;Read all of the image file names        filenames=strarr(n_images)        for i=0, n_images-1 do begin                tmp=''                readf, un, tmp                filenames(i) = tmp        endfor;;Read all of the image dates        dates=intarr(3,n_images)        for i=0, n_images-1 do begin                j=0                tmp=''                readf, un, tmp                tmp=strsplit(tmp, /extract)                if n_elements(tmp) lt 3 then return, {n_images:-2}                for k=0, 2 do begin                        reads, tmp(k),j                        dates(k,i) = j                endfor        endfor;;read in the remaining file names        sandfile=''        readf, un, sandfile        rangefile=''        readf, un, rangefile        centdir=''        readf, un, centdir        winddir=''        readf, un, winddir        c3c4file=''        readf, un, c3c4file        close, un        free_lun, un;;return the result in a simple structure        return, {n_images:n_images,filenames:filenames, dates:dates, $                sandfile:sandfile, rangefile:rangefile, centdir:centdir,$                winddir:winddir, c3c4file:c3c4file}endpro LaTeXLandsatTable	outfile = 'LandsatTable.tex'	openw, oun, /get, outfile	printf, oun, '\begin{tabular}{|c|c|}'	printf, oun, '\hline'	printf, oun, 'Landsat Scene & Image Date\\'	printf, oun, '\hline'	printf, oun, '\hline'	scenes=findfile('*')	cd, current=masterDIR	print, scenes	for i=0, n_elements(scenes)-1 do begin		print, scenes(i)		current=strtok(scenes(i),':', /extract)		if (strtok(scenes(i), '.'))(0) eq 0 and file_test(current(0), /directory) then begin			cd, current(0)			print, ' ', string(byte(13))			print, '**************************',current(0),'**************************'			if file_test('ModelInput') then begin				info=readInputfile('ModelInput')				for j=0, info.n_images-1 do $					printf, oun, strcompress(string(current), /remove_all), ' & ', $					strcompress(string(info.dates(1,j),'/',info.dates(2,j),'/',info.dates(0,j)), /remove_all), '\\'			endif						cd, masterDIR		endif	endfor	printf, oun, '\hline'	printf, oun, '\end{tabular}		close, oun	free_lun, ounend